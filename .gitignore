# Dependencies
node_modules/
.pnpm-store/

# Build output
dist/
build/
out/
*.tsbuildinfo

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Environment / secrets
.env
.env.local
.env.*.local
!.env.example
# .env.production do frontend Vite contem URLs PUBLICAS (sem secrets)
# e precisa ir pro repo pra builds de CI / Netlify funcionarem.
!apps/frontend/.env.production

# Editor / OS
.vscode/*
!.vscode/extensions.json
!.vscode/settings.example.json
.idea/
.DS_Store
Thumbs.db
*.swp
*.swo

# Storage de firmware (uploads do backend)
apps/backend/storage/
!apps/backend/storage/.gitkeep

# Prisma
apps/backend/prisma/migrations/dev.db*
*.db
*.db-journal

# Docker volumes locais (caso usuário rode docker compose com volume bind)
infra/data/

# Cobertura de testes
coverage/

# Tauri (futuro)
apps/desktop/src-tauri/target/

# Frontend dist (deploy Netlify)
apps/frontend/dist/

# Pacotes locais empacotados (npm pack)
*.tgz

# Pacote de deploy gerado pra Netlify drag-and-drop
synapse-frontend-deploy.zip
